import React, { Component } from 'react';

class PriceSummary extends Component {
  render() {
    const { subTotal, pickupSavings, taxes, total } = this.props;
    return <div />;
  }
}

export default PriceSummary;
